let value = new Map([
    ["1", 2],
    ["3", 4],
])
function main(label) {
    const x = 1
    if (x === 1) {
        const label = "Test"
        console.log(label)
    }
}

for (let i = 0; i < 3; i++)
const x = 1
